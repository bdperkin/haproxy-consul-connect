# Generated by go2rpm 1.3
%bcond_without check

# https://github.com/tidwall/buntdb
%global goipath         github.com/tidwall/buntdb
Version:                1.2.3

%gometa

%global common_description %{expand:
Package buntdb implements a low-level in-memory key/value store in pure Go. It
persists to disk, is ACID compliant, and uses locking for multiple readers and
a single writer. Bunt is ideal for projects that need a dependable database,
and favor speed over data size.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        1%{?dist}
Summary:        Package buntdb implements a low-level in-memory key/value store in pure Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/tidwall/btree)
BuildRequires:  golang(github.com/tidwall/gjson)
BuildRequires:  golang(github.com/tidwall/grect)
BuildRequires:  golang(github.com/tidwall/match)
BuildRequires:  golang(github.com/tidwall/rtred)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/tidwall/lotsa)
%endif

%description
%{common_description}

%gopkg

%prep
%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog

