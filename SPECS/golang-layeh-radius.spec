# Generated by go2rpm 1.3
%bcond_without check

# https://github.com/layeh/radius
%global goipath         layeh.com/radius
%global forgeurl        https://github.com/layeh/radius
%global commit          838e26d0c9be1b92b9e6e8b8ad0888e9d8e7ec4f

%gometa

%global common_description %{expand:
A Go (golang) RADIUS client and server implementation.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        0.1%{?dist}
Summary:        A Go (golang) RADIUS client and server implementation

# Upstream license specification: MPL-2.0
License:        MPLv2.0
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(golang.org/x/crypto/md4)
BuildRequires:  golang(golang.org/x/text/encoding/unicode)

%description
%{common_description}

%gopkg

%prep
%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done
for cmd in debug vendors/microsoft; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog

